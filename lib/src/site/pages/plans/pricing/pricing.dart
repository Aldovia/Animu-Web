class Pricing {
  final String title;
  final String desc;
  final double price;
  final bool isAvailable;
  final bool isMain;

  Pricing({this.title, this.desc, this.price, this.isAvailable, this.isMain});
}
