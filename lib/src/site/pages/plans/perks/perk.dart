class Perk {
  final String title;
  final String desc;
  final String icon;
  final bool isFA;
  final bool isPlus;
  final bool isPro;

  Perk({this.title, this.desc, this.icon, this.isFA, this.isPlus, this.isPro});
}
